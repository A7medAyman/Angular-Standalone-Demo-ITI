import { Cardshadow } from './cardshadow';

describe('Cardshadow', () => {
  it('should create an instance', () => {
    const directive = new Cardshadow();
    expect(directive).toBeTruthy();
  });
});
